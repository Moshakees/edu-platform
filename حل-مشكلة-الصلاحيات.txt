═══════════════════════════════════════════════════════════
        ⚠️ مشكلة في صلاحيات GitHub
═══════════════════════════════════════════════════════════

❌ الخطأ:
   Permission denied to Moshakees/edu-platform.git

📝 السبب:
   المستخدم الحالي (abody1217) لا يملك صلاحية 
   الوصول للمستودع Moshakees/edu-platform

═══════════════════════════════════════════════════════════
✅ الحلول الممكنة:
═══════════════════════════════════════════════════════════

الحل 1️⃣ - تسجيل الدخول بحساب Moshakees:
───────────────────────────────────────────────────────────
1. افتح Git Bash أو Terminal
2. نفذ:
   git config --global user.name "Moshakees"
   git config --global user.email "your-email@example.com"

3. ثم حاول Push مرة أخرى:
   git push -u origin main

4. سيطلب منك Username و Password:
   - Username: Moshakees
   - Password: استخدم Personal Access Token
     (ليس كلمة المرور العادية!)


الحل 2️⃣ - إنشاء Personal Access Token:
───────────────────────────────────────────────────────────
1. اذهب إلى GitHub.com
2. Settings > Developer settings > Personal access tokens
3. Generate new token (classic)
4. اختر الصلاحيات:
   ✓ repo (كل الصلاحيات)
5. Generate token
6. انسخ الـ Token (سيظهر مرة واحدة فقط!)

7. عند Push، استخدم:
   Username: Moshakees
   Password: [الصق الـ Token هنا]


الحل 3️⃣ - استخدام SSH بدلاً من HTTPS:
───────────────────────────────────────────────────────────
1. أنشئ SSH Key:
   ssh-keygen -t ed25519 -C "your-email@example.com"

2. أضف الـ Key لـ GitHub:
   - انسخ محتوى: ~/.ssh/id_ed25519.pub
   - GitHub > Settings > SSH Keys > New SSH key

3. غيّر الـ remote:
   git remote remove origin
   git remote add origin git@github.com:Moshakees/edu-platform.git
   git push -u origin main


الحل 4️⃣ - إنشاء مستودع جديد بحسابك:
───────────────────────────────────────────────────────────
إذا كان المستودع ملك شخص آخر:

1. أنشئ مستودع جديد بحسابك (abody1217)
2. غيّر الـ remote:
   git remote remove origin
   git remote add origin https://github.com/abody1217/edu-platform.git
   git push -u origin main

═══════════════════════════════════════════════════════════
🔧 الأوامر السريعة:
═══════════════════════════════════════════════════════════

# معرفة الـ remote الحالي
git remote -v

# حذف الـ remote
git remote remove origin

# إضافة remote جديد
git remote add origin https://github.com/USERNAME/REPO.git

# Push
git push -u origin main

═══════════════════════════════════════════════════════════
💡 التوصية:
═══════════════════════════════════════════════════════════

الأسهل: استخدم Personal Access Token (الحل 2)

1. أنشئ Token من GitHub
2. عند Push، استخدم Token كـ Password
3. سيتم حفظ البيانات تلقائياً

═══════════════════════════════════════════════════════════

❓ هل تريد المساعدة في خطوة معينة؟

═══════════════════════════════════════════════════════════
